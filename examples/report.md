title: The other castle document
eyebrow: MEDIATRON · INTERNAL TARGET

*The internal link target. Rendered alongside its caller so a relative
link opens in the same window.*

This page exists so the milestone's "one link to another castle
document opens in the same window" has a target to land on. The
renderer found the relative `report.md` link, rendered this page to
`report.html`, and rewrote the link to point at it.

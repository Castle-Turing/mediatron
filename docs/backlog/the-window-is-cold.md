# The window is cold

Every `mediatron` invocation pays browser startup. The founding
direction anticipates a warm instance waiting headless so pages feel
instant — deferred, per [m1-out], until the cold-open latency is
measured on the real machine rather than assumed. Whoever specs this
measures first: if cold-open is already under a perceptual
threshold, this entry dies happily.

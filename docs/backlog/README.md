# docs/backlog — deferred work, in plain text

One file per item, named as a statement of the problem. Speccing an
item promotes it to a numbered brief in docs/tasks/ and deletes the
backlog file in the same commit.
